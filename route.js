/**
 * Legacy reference — campaign API is served from pages/api/campaign.js (Pages Router).
 * Shared streaming logic lives in lib/campaign.js.
 */

export { CAMPAIGN_API_INFO, streamCampaign } from './lib/campaign.js'
